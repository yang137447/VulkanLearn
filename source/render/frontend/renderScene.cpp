#include "render/frontend/renderScene.h"

// RenderScene is currently a pure data contract. Keeping this translation unit
// in the build catches include and dependency drift as the renderer migrates.
